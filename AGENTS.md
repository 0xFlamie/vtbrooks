# vtbrooks — VT 投票 + AI 主决策信号机器人

## 架构（v4.0, 2026-08-07）

单文件 `vt_vote_bot.py`，systemd 服务 `vtbrooks`（服务器 ccvps, `/opt/vtbrooks`）。

定位：**信号是用户主观交易的趋同性暗示，不是自动开单指令**（用户自管仓位和止损）。

双层 AI 架构（两层数据隔离，各自判决，互不参考）：

| 层 | 数据 | AI 输出 | 回答 |
|---|---|---|---|
| 4h 层 | 趋势排列(EMA7/25/99)/Brooks4h/挤压分位/威科夫 TR/合约情绪 | 方向 + 幅度档(<1%/1-2%/2-3%/3%+) + 置信 | 有没有大行情，往哪边，多大 |
| 15m 层 | 18因子投票/15m形态/关键位/RSI/量比/VWAP/合约情绪 | 方向 + 置信 | 现在是不是入场点 |

判决频率：**4h 层每根 4h 收线重判一次（缓存，`judge4_cache`），15m 层每 3 分钟判**——4h 输入在根内不变，高频重判只会制造抖动（2026-08-06 反手抖动事故的根因）。

实时快检（2026-08-09，检测实时化/判决触发式）：两次 3 分钟全扫之间每 10 秒拉 Coinbase ticker（`fetch_fast_price` + `fast_ref` 基准），急动（0.5×ATR 阈值）/破摆动极值/穿 VWAP → 立即全扫 + AI 判决 + 推送。AI 调用次数不随快检频率增长，成本不变。

4h 判决迟滞（2026-08-08）：重判时把上一根判决（方向/置信/幅度档）注入简报末尾，SYS_4H 要求"证据没有明显变化就维持原方向，翻转必须写清新证据"——临界区证据接近 50/50 时，AI 曾因一根新 K 线 180° 改向且置信度不变。

推送（无快报，全部是信号卡 `format_layers`）：

- **定时信号**：:00/:15/:30/:45 各推一条
- **反转加推**：3 分钟扫描中任一层方向与上次不同 → 立即加推
- **异动加推**：RSI(15m+4h) 穿越 70/30、量比突破 2、逼近压力/支撑（<0.3%）、挤压进出 <20% 区、3 分钟急涨急跌（阈值 0.5×15m ATR，夹 0.2-0.6%，随波动率自适应）、VWAP 穿越、突破/跌破近 20 周期摆动极值、入场窗口开启（边沿触发）
- **新闻加推（2026-08-10）**：Axios/CoinDesk/CoinTelegraph RSS 每 3 分钟扫描，关键词白名单（地缘/联储/SEC/ETF/BTC/ETH 等）命中即推 `📰` 并进 15m 简报给双模解读；link 去重存 `runtime_state.json` 的 `news_seen`（保留 500 条）
- **统计形态触发（2026-08-10，五年数据训练）**：费率≥0.03%+7日跌>5%→偏空90%；费率≥0.05%+7日涨>5%→偏空87%；7日涨>5%+挤压<20%→偏多87%。文案带历史概率，喂双模解读
- **异动由 AI 解读（2026-08-08）**：异动检测保持规则化（`detect_events`，确定性），但不再只配死文案——检测结果注入 15m 层简报，SYS_15M 要求结合走势解读（同样的 RSI 超买，趋势里=动能、区间顶缩量=回落前兆），解读体现在 15m 层 📝 理由；卡片 ⚡ 行仍列规则原文。15m 层也注入上次判决做迟滞（`judge15_prev`）
- **入场窗口（`entry_window`，纯规则不经 AI）**：4h 有方向为前提，A 回调=贴近结构位（≤0.5×ATR15m）+RSI 重置/形态扳机，B 突破=收破摆动极值+放量>1.5 倍；输出入场区/失效位/距离%
- 卡片含 💬 白话段（规则生成，概率只引 `market_stats` 多年统计库）、双层 emoji 方向、✅共振/⚠️背离标注
- **交易员观点段（2026-08-20）**：卡片顶部 4-6 行——🎯观点（方向+置信+幅度档）→ 📌计划（仓位%+入场+止损距离%+目标+RR+**单笔风险%**）→ 🚫失效（结构位/入场窗口失效位，认错翻边）→ 🔁上次观点（维持/翻转+新证据）。仓位规则表（2026-08-22 加波动率/事件修正）：幅度档×共振×置信 → ATR 修正（∝1/ATR，基准3%）→ 挤压分位修正（≥70 减仓、<20 加仓）→ 事件日七折；封顶 50%。连亏≥3 单卡片降级提示（`loss_streak`）
- ~~每日复盘信~~（2026-08-23 随机械结算一起删除）
- **分析结论（2026-08-22，analysis/，OKX 现货 5年 11100 根 4h + Coinbase 5年，全部时间切分验证）**：
  **已确认 edge（可交易/可进生产）**：
  ① 波动率聚集（最强）：前4h振幅 >3.5% → 未来12h振幅 +53%；<1% → -34%（振幅自相关，比挤压分位强一倍）
  ② 挤压分位 → 12h振幅：≥70 +21~25%、<20 -15~16%（样本外三段稳定）
  ③ 事件日波动：FOMC/CPI 日振幅 +27%（99 样本），方向无偏差
  **弱信号（记录，不可独立交易，EV≈0）**：
  ④ 收盘位置(4h内)：低位20% → 未来涨 43.9%，高位80% → 54.0%（日内强度延续，单调、样本大）
  ⑤ 距20期极值：贴近高点 → 53.2%，贴近低点 → 44.6%（同向延续）
  ⑥ 均值回归：4h大跌≤-2%后做多胜率 56.6%（后段衰减 52.1%）但平均收益 -0.02%（尾部深跌吃掉），配止损仍 EV≤0
  ⑦ ETH/BTC 相对动量：ETH 弱于 BTC 后未来涨 46.5-47%（延续，-3pp，两段一致）
  ⑧ BTC 绝对联动（第四轮）：BTC 前4h涨0.5-2% → ETH 未来涨 53.1%，BTC 跌 → 44.8%（动量延续，+4~8pp，单调样本大，但疑为 ETH 自身动量代理且 EV≈0）
  ⑨ 20期突破延续（第四轮）：突破高点 → 未来12h +0.30%、跌破低点做空 +0.15%（胜率仅 +1~4pp，收益靠右尾，实盘滑点风险大）
  ⑩ 弱信号合成（第五轮）：强度分+2 → 未来涨 54.7%（5轮最强方向信号）、-2 → 41.2%（单调 +14pp），但含成本 EV 全负——方向收益层面定论：不可交易
  ⑪ 振幅预测模型（第五轮）：LightGBM(range/ATR/|ret|) → 未来12h振幅 RMSE 1.96% vs 基准 2.13%，预测分位跨度 1.8x——可进生产的波动率管理工具
  **证伪（勿重复投入）**：
  ⑫ 4h 方向预测：15m 窗口形态 IC 全≈0、LightGBM 组合 AUC=0.497→0.533（弱信号集）、RSI 极值/时段/跨周期动量/量价背离均无
  ⑬ EMA 趋势排列（生产 4h 层核心输入）对未来方向无预测力（多头 49.6% vs 基线 49.4%）
  ⑭ 波动率对方向无调节（高波动大跌后胜率 47.4%，不反弹）；HL 费率极端样本不足无法验证（HL 费率比 Binance 温和）
  数据源：OKX 4h 现货 5年（11100 根）优于 Hyperliquid（仅 2.3 年）；Coinbase 1h/15m 历史 2021+ 完整但分页慢（限流时 ssl read 可挂起，需 requests (connect,read) 超时）
- 卡片含双层 emoji 方向、幅度档、因子看涨百分比、✅层级共振/⚠️层级背离标注
判例本（`record_judge`）：只记原始判决（方向/置信/理由/价格/状态），双层同向 1h 去重，上限 500。**机械结算已删除（2026-08-23 用户决策：15m 平仓时间随机+扛单，判对错是假精确）**。

复盘：AI 叙事复盘（`maybe_update_lessons`）——每积累 20 条新判决，AI 读自己最近的判决 + 4h 后实际涨跌（原始 %，不打分），写 3-5 条人话教训存 `judge_lessons.json`，**注入两层简报**（裁判据此自我校准；推翻 2026-08-05 "历史不注入" 决策）。

## 数据源（服务器为美区 IP，注意 geo-block）

| 数据 | 主源 | 兜底 |
|---|---|---|
| K线(USDC对) | Coinbase ETH-USD | fapi → yfinance |
| K线(USDT对) | api.binance.us | Coinbase → fapi → yfinance |
| 合约情绪 | Binance fapi（服务器 451 被封） | **OKX**（免 key 美区可用：费率/持仓/持仓1h/多空比）→ Kraken → Hyperliquid |
| 裁判 | DeepSeek API（`VT_DS_API_KEY`） | 失败→观望(fail-closed)+WARN 日志 |

已知坑：

- **Binance.US 的 USDC 对是死盘**（ETHUSDC 24h 成交个位数 ETH，最新价偏离数美元），USDC 对必须跳过，见 `fetch_klines`
- fapi.binance.com 美区 451；Bybit 美区 CloudFront 封；OKX/Hyperliquid/Kraken/Gate 美区可用
- CoinAnk 有付费 API（多空比/清算地图等 80 接口），未接入，免费方案已够用

## 运行环境

- 环境变量：`.env`（systemd EnvironmentFile），含 `VT_DS_API_KEY`、Telegram token/chat —— 永不入库
- 运行时文件（未跟踪）：`judge_journal.json` / `judge_lessons.json` / `vt_predictions.json` / `oi_snapshots.json`（HL 持仓快照兜底用）

## 部署链（强制）

本机验证（py_compile + 单测）→ commit → push → 服务器 `git pull --ff-only` → `systemctl restart vtbrooks`。
禁止 scp 直推、禁止服务器上直接改代码。回滚：`git reset --hard <sha>` + restart。

## 监控

```bash
ssh ccvps 'journalctl -u vtbrooks -f'                 # 实时日志
ssh ccvps 'journalctl -u vtbrooks --since today | grep WARN'  # 裁判失败记录
```

复盘：`judge_journal.json` 只留原始判决记录；教训在 `judge_lessons.json`（AI 叙事复盘生成）。

## 当前状态（2026-08-22）

- 分析完成（analysis/）：`feature_explore.py`（Coinbase 5年 10946 窗口, 单因子IC+条件概率+时间切分回测, features.csv 缓存）、`train_model.py`（LightGBM, AUC=0.497）、`squeeze_test.py`（挤压→振幅, 5年三段稳定）、`event_test.py`（事件日 +27% 波动）
- 生产新增：仓位波动率/事件修正（ATR∝1/ATR、挤压≥70×0.7/<20×1.1、事件日×0.7，5 年数据确认参数有效）、卡片风险%行
- 2026-08-22 晚：判对错展示已移除（用户决策：无真实成交+插针下判定失真）——daily_review 去判对率/错题/幅度档/累计判对率，改方向分连续统计；loss_streak 连亏提示删除；方向分仍落盘供分析
- 叙事修正（回测驱动）：挤压<20 不是“大行情酝酿”（未来12h振幅反而-15%），`sq_word`/异动文案已改；卡片加“🌊 波动: 未来12h预计振幅”行（挤压分位→振幅映射表）
- 已知待改：AGENTS.md/文案里“近5年”统计应改为实际 2.3 年（Hyperliquid）或 OKX 5 年（分析用）；`market_stats` 样本实际 2.3 年
- 已知待改：生产 `sq_word` “挤压<20=大行情酝酿”叙事与回测相皮（实际未来12h更平静）；AGENTS.md/文案里“近5年”统计应改为 2.3 年；`market_stats` 样本实际 2.3 年
- 已有（2026-08-20）：结算诚实化（观望不评判对率/幅度档反向基准）、交易员观点段、每日复盘信、新闻快检因果回溯
- 下一步候选：事件日方向数据积累（2b 延伸）；波动率预测进简报（若挤压 edge 落地）；移除 prompt “置信度用满量程”
